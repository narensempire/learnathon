# HTML
- task: AppPoolStopStart@2
  inputs:
    machinesList: 'ec2-54-67-80-52.us-west-1.compute.amazonaws.com'
    AdminUserName: 'NAREN-HP\Administrator'
    AdminPassword: 'UCLYCUPaqe%vF@Li@hw--JI9)44GrTeW'
    ApppoolName: 'abc'
    stopstart: 'STOP'
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(build.sourcesDirectory)'
    includeRootFolder: false
- task: PublishBuildArtifacts@1
